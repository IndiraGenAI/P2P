{
  "name": "erp-db",
  "version": "0.0.1",
  "description": "",
  "author": "IndiraGenAI",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "db:pack": "npm pack",
    "build": "rimraf dist && tsc",
    "deploy": "sh updateScript.sh",
    "build:pack": "npm run build && npm run db:pack",
    "build:pack:deploy": "npm run build:pack && npm run deploy",
    "db": "typeorm-model-generator -h 192.168.29.101 -p 5432 -d rnw-erp  -u postgres -x postgres -e postgres -o output --cf param --noConfig true",
    "db:dev": "typeorm-model-generator -h 103.154.233.221 -p 5432 -d erp_db -u erp_user -x ERP@100491 -e postgres -o output --cf param --noConfig true"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typeorm-model-generator": "^0.4.6",
    "typescript": "^4.3.5"
  },
  "dependencies": {}
}
