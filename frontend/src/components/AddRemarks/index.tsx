import React from "react";
import { IRemarksProps } from "./AddRemarks.model";

/**
 * Full remarks UI depends on admission/course modules not present in this
 * workspace snapshot. This stub keeps dependent pages type-checking.
 */
const Remarks = (_props: IRemarksProps) => null;

export default Remarks;
